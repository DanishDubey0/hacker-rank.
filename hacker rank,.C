// WAP TO CONVERTE CELSIUS INTO FARANHIET
#include<stdio.h>
float converttemperature(float celsius);

int main()
{
float far = convert temp(0);
printf("far: %f",far);
return 0;
}
float convert temperature(float celsius)
{
    float far = celsius * (9/5) + 32;
    return far;
}
